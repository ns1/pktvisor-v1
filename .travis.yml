language: c
compiler:
  - gcc
  - clang

before_install:
  - sudo apt-get update -qq
  - sudo apt-get install -qq libgeoip-dev liburcu-dev libpcap-dev libnet1-dev libcli-dev

script: CC=$CC ./configure && make CC=$CC
